version: 2

updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # One PR per area instead of one per package — the toolchain moves together.
      dev-tooling:
        patterns:
          - "@biomejs/*"
          - "@changesets/*"
          - knip
          - simple-git-hooks
          - tsup
          - typescript
          - vitest
          - "@vitest/*"
      react:
        patterns:
          - react
          - react-dom
          - "@types/react"
          - "@types/react-dom"
    ignore:
      # Peer range is >=18 and CI proves it; a major bump here is a deliberate decision.
      - dependency-name: react
        update-types: [version-update:semver-major]
      - dependency-name: react-dom
        update-types: [version-update:semver-major]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
